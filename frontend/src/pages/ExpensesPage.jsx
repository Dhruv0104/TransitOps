import PagePlaceholder from '../components/PagePlaceholder'

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Fuel & Expenses"
      description="Fuel logs (liters, cost, date) and other expenses. Operational cost = Fuel + Maintenance per vehicle."
    />
  )
}
